using System.Collections;
using System.Collections.Generic;
using UnityEngine;
namespace as18 {
    public class Test : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            // GameContainer<string> gCont = new GameContainer<string>();
            // gCont.Item = "Healing Potion";
            // Debug.Log(gCont.Item);
            // Debug.Log(GameUtils.DescribeItem<string>((string) gCont.Item));
        }

    }
}
